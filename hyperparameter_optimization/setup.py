from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='Example project on how to use different hyperparameter optimization algorithms for machine learning. The main focus is bayesian optimization with gaussian processes and TPE',
    author='robin',
    license='',
)
